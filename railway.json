{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS"
    },
    "deploy": {
      "startCommand": "env/bin/python -m ensurepip --upgrade && pip install -r requirements.txt && env/bin/python manage.py migrate && env/bin/python manage.py collectstatic --noinput && gunicorn mysite.wsgi",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
  }